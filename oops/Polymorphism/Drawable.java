package oops.Polymorphism;

/**
 * INTERFACE POLYMORPHISM
 *
 * A reference of interface type can point to any object that implements it.
 * This helps us write flexible code.
 */
interface Drawable {
    void draw();
}
